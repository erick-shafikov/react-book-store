import styled from 'styled-components';
import img from '/background.jpg';

export const StyledWrapper = styled.div`
    display: grid;
    gigrid-template-rows: repeat(auto-fill, minmax(100px, 1fr));
`;

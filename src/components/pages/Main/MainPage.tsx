import { BookList } from '../../organisms/booklist';
import { Styled } from './styled';

export const MainPage = () => {
    return (
        <Styled>
            <BookList />
        </Styled>
    );
};
